# Zig build artifacts
zig-out/
.zig-cache/

# macOS
.DS_Store

# Local user config (machine-specific paths)
.claude/
.codex/
.mcp.json

# Editor
*.swp
*.swo
*~
.vscode/
.idea/

# Local progress scratch files should not be tracked in this or nested repos.
progress.md

# Nested Delta project metadata is local state for imported repos.
*/.delta/

# Delta runtime ledger is local state; commit Delta project config, not ledger events.
.delta/ledger/

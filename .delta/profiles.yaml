# Generated by Delta project init.
# Edit this project-local file to add team members.
# Team mode routes each turn to selected profiles; explicit all-team requests fan out.
# Use /profile PROFILE_ID to set the focus/default profile for single mode and manual workers.
team_mode: true
profiles:
  user:
    id: user
    role: user
    name: "keza"
  delta:
    id: delta
    role: lead
    name: Delta
    description: Lead orchestrator profile for this project.
    mode: inherit
  # backend:
  #   id: backend
  #   role: agent
  #   name: Backend
  #   description: Owns API, data, auth, storage, and integration work.
  #   model: gpt-5
  #   reasoning_effort: high
  #   mode: inherit
  # frontend:
  #   id: frontend
  #   role: agent
  #   name: Frontend
  #   description: Owns app screens, UI state, styling, and client integration.
  #   model: gpt-5
  #   reasoning_effort: medium
  #   mode: inherit
  # marketing:
  #   id: marketing
  #   role: agent
  #   name: Marketing
  #   description: Owns positioning, launch copy, campaigns, and audience research.
  #   mode: inherit
  # sales:
  #   id: sales
  #   role: agent
  #   name: Sales
  #   description: Owns ICP, pricing notes, outreach, distribution, and objection handling.
  #   mode: inherit
